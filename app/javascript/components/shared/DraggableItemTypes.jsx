/**
 * The type of elements that can be dragged.
 * It must be used to configure both the Draggable and the DropZone components.
 */
export const DraggableItemTypes = {
  /**
   * Represents a set of properties.
   */
  PROPERTIES_SET: 'properties-set',
  /**
   * Represents a set of concepts inside a concept scheme
   */
  CONCEPTS_SET: 'concepts-set',
};
