/**
 * Style so that the card manages to stand out above the surrounding components
 */
export const transparentCardStyle = {
  position: 'absolute',
  minWidth: '-webkit-fill-available',
  zIndex: '3',
};
