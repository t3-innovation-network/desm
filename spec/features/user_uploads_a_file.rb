# frozen_string_literal: true

# First step of the mapping is "Upload a specification", so
# here we will cover that steps as a feature
require "rails_helper"

feature "User uploads a specification" do
  scenario "they can upload a specification" do
    # visit mappings_index

    # @todo: Complete the test with the proper validation when it's commited

    # expect(page).to
  end
end
