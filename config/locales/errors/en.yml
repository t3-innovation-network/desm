en:
  errors:
    general: "Attention: %{message}"
    not_found: "Record not found"
    record_invalid: "Invalid: %{message}"
    record_not_destroyed: "Can't delete: %{message}"
    record_not_unique: "Record not unique: %{message}"
    invalid_json: "The provided value is not a valid JSON"
    email_account_not_configured: "The email account provided as the sender might not be correctly configured. Please check your credentials and provider configuration. %{message}"
    auth:
      unauthorized_access: "Unauthorized access."
      invalid_credentials: "Invalid email or password. Please try again."
    config:
      configuration_profile_user:
        destroy:
          one: "Can't delete agent %{message} as there are some mappings done by that agent. Changes were not applied, please remove the mappings first."
          other: "Can't delete agents %{message} as there are some mappings done by those agents. Changes were not applied, please remove the mappings first."
        update:
          one: "Email %{message} is already in use for this organization. Changes were not applied."
          other: "Emails %{message} are already in use for this organization. Changes were not applied."
      domain:
        destroy:
          one: "Can't delete abstract class %{message} from previous scheme as it is in use. Changes were not applied."
          other: "Can't delete abstract classes %{message} from previous scheme as they are in use. Changes were not applied."
      organization:
        destroy:
          one: "Can't delete organization %{message} as it has mappings or/and agents. Changes were not applied, please remove the mappings and agents first."
          other: "Can't delete organizations %{message} as they have mappings or/and agents. Changes were not applied, please remove the mappings and agents first."
        update:
          one: "Organization name %{message} is already in use for this configuration profile. Changes were not applied."
          other: "Organization name %{message} are already in use for this configuration profile. Changes were not applied."
      predicate:
        destroy:
          one: "Can't delete predicate %{message} from previous sheme as it is in use. Changes were not applied."
          other: "Can't delete predicates %{message} from previous sheme as they are in use. Changes were not applied."
    specs:
      invalid_specification: "Invalid specification. Please validate the uploaded file. It has to be a valid JSON-LD, XML, RDF, or CSV file.s"
    synthetic:
      spine_term_not_uniq: "Can't create syntetic property as term %{term} already exists in the spine. Changes were not applied."
  pundit:
    default: You are not authorized to perform this action.